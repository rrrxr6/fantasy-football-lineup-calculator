package roach.ryan.ff.model;

public class RunningBack extends Flex
{
    public RunningBack(String name, int salary, int rank, double projectedPoints, double actualPoints)
    {
        super(name, salary, rank, projectedPoints, actualPoints);
    }
}
