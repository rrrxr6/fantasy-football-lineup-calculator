package roach.ryan.ff.model;

public class TightEnd extends Player
{
    public TightEnd(String name, int salary, int rank, double projectedPoints, double actualPoints)
    {
        super(name, salary, rank, projectedPoints, actualPoints);
    }
}
