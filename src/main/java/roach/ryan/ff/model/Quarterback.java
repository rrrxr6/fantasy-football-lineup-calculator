package roach.ryan.ff.model;

public class Quarterback extends Player
{
    public Quarterback(String name, int salary, double projectedPoints, double actualPoints, int rank)
    {
        super(name, salary, projectedPoints, actualPoints, rank);
    }
}
